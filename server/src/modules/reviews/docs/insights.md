# Insights — server/modules/reviews

Knowledge you can't see in the code. Newest entry on top of each section.
Format and rules: `.claude/skills/engineering-insights/SKILL.md`.

## What Works

- **2026-09-18 · Check a run's actual inputs before re-running** — The trace's `prompt_assembly` records exactly what the model saw: `system` (which agent prompt version), `user` (the diff), and `pr_description`. Three substring checks settle "did my change reach the model?" in seconds, e.g. `'duplicated magic constant' in system`, `'<new identifier>' in user`, `pr_description[:160]`. It caught that a run used the new prompt and the new file but a stale PR body.
  Where: `routes.ts:121` (`GET /runs/:id/trace`), `run-executor.ts:273` (`prompt_assembly: outcome.assembly`), `reviewer-core/src/prompt.ts:129` (`assembly`; `pr_description` at `:136`)

- **2026-09-18 · `agent_runs.grounding` tells "model found nothing" apart from "we dropped it"** — When a review lands with 0 findings, read the run's `grounding` string before blaming the pipeline: `0/0 passed` = the model returned an empty `findings` array (nothing to ground), `0/1 passed` = it proposed one finding and the citation gate dropped it for not intersecting a real diff hunk. Same query gives the prompt size: `SELECT grounding, findings_count, tokens_in||'->'||tokens_out FROM agent_runs WHERE status='done' ORDER BY ran_at DESC`.
  Where: `run-executor.ts:250` (`grounding` written on `done`), `server/src/db/schema/runs.ts:29` (`grounding` column), `reviewer-core/src/grounding.ts:87` (`groundingSummary`)
  **Correction (2026-09-19):** failed and cancelled runs ALSO store `0/0 passed` (`run-executor.ts:84` in `failAll`, `run-executor.ts:306` in the catch block), so the reading above only holds for `status='done'` rows — keep that filter in the query.

## What Doesn't Work

- **2026-09-18 · Labelling code "demo / fixture / never imported" suppresses every lower-severity finding** — Tried: a reviewer fixture whose file header, PR title and PR body all said "intentionally flawed, never imported, do not ship", planted with 2 CRITICAL + 2 WARNING + 2 SUGGESTION issues. Failed because: the model (Sonnet 5, even after the SUGGESTION prompt fix below) reported only the real security defects. Its summary spelled out why — `INJECTION_GUARD` and the task line say such claims can't waive REAL defects, so it reported those, and treated maintainability issues in a declared-dead file as irrelevant ("dead code" means nothing when the whole module is announced as never imported). Instead: make fixtures read like ordinary production code — neutral header, neutral PR title/body — and make each low-severity issue provable from the diff alone. Also: an **exported** function is never "dead code" to the reviewer (it's public API that may be used elsewhere); plant a **non-exported** helper that nothing in the file calls. **The PR description alone is enough to suppress them:** a follow-up run had the fixed prompt AND a neutral file header, yet the stale PR body ("A deliberately flawed, never-imported module…") still yielded 2 CRITICAL + 1 WARNING + 0 SUGGESTION, and the model's summary cited the description by name. Fix the description BEFORE merging — once a PR is merged its text is frozen for this purpose, and a fresh PR is the only way to re-run cleanly.
  Where: `demo/findings-code-example.ts:71` (`formatQuotaLabel` — the planted non-exported, never-called helper), `reviewer-core/src/prompt.ts:16` (`INJECTION_GUARD`), `helpers.ts:82` (`taskLine`)

- **2026-09-18 · "Every run returns exactly 3 findings" was not a cap** — Tried: explaining zero SUGGESTIONs by a findings limit, since three runs on two models each returned exactly 3. Failed because: nothing caps, thresholds or rewrites findings anywhere (`grounding.ts`, `reduce.ts`, `to-review.ts`, `run.ts:208`); the raw output simply contained 3. The count was a coincidence of the fixture. Instead: read the run trace's `raw_output` before theorising about the pipeline — it shows exactly what the model emitted, pre-grounding.
  Where: `reviewer-core/src/review/run.ts:208` (`findings: ground.kept` — no cap), `run-executor.ts:280` (`raw_output: outcome.raw`), `routes.ts:121` (`GET /runs/:id/trace`)

- **2026-09-18 · `deepseek/deepseek-v4-flash` yields no grounded findings — don't demo findings features on it** — Tried: showing the severity-counter feature with the built-in agents (all three seed agents use this model via OpenRouter). Failed because: 7/7 runs across both seeded and real PRs produced `findings_count = 0` — mostly `0/0 passed`, and on the 4465-line PR the model answered with 165 output tokens against a 25K-token prompt (it effectively gave up). Instead: for any demo/verification that needs real findings, point one agent at a stronger model (e.g. `anthropic/claude-haiku-4.5`, `anthropic/claude-sonnet-5` — both on OpenRouter) and run it on the SEEDED PR #482, whose diff carries a planted `stripeKey: "sk_live_xxx"` on a real diff line, so the finding can cite it and survive grounding. Cost is ~$0.01 per run at that prompt size.
  Where: `server/src/db/seed.ts:13` (`DEFAULT_MODEL`), `server/src/db/seed.ts:120` (PR #482 `pr_files` — no `patch`), `diff-loader.ts:29` (`diffFromPrFiles` fallback), `diff-loader.ts:37` (`if (!f.patch) continue`)
  **Correction (2026-09-19):** the recommendation to use seeded PR #482 is wrong. Its `pr_files` rows have **no `patch`** (`server/src/db/seed.ts:120`), and the seeded repo is never cloned, so `loadDiff` falls back to `diffFromPrFiles` (`diff-loader.ts:29`), which skips every patch-less file (`diff-loader.ts:37`) → the model gets an EMPTY diff and any model scores `0/0 passed` (confirmed: the 3 newest `done` runs on #482 — one `claude-sonnet-5`, two `deepseek-v4-flash` — all have a 746-char user prompt with `## Diff to review` at offset 690, i.e. an empty diff). `stripeKey: "sk_live_xxx"` exists only in the test mocks (`server/src/adapters/mocks.ts:177`, `MockGitHubClient`), never in the seed. So the "7/7 runs → 0 findings" above mixes two causes: #482 runs prove nothing about the model. Instead: demo findings on a real imported PR with a real diff (e.g. the `demo/` fixture PRs on `DanceWithBikes/dev-digest`), and check the trace's `prompt_assembly.user` is non-empty first.

## Codebase Patterns

- **2026-09-18 · The General Reviewer prompt made SUGGESTION unreachable (fixed)** — It defined SUGGESTION as "a minor improvement or nit" and then forbade nits twice ("No style nits" in Quality bar; "not a license to report style nits" under Clarity), so the model never emitted the level at all — yet `scoreFromFindings` penalises SUGGESTION (−3), `Review.score`'s description names "minor suggestions", and the UI renders a SUGGESTION pill. Fix: SUGGESTION now lists concrete in-bounds cases (duplicated magic constant, dead/unreachable code, misleading name or doc on correct code, redundant check) and the ban is narrowed to *formatting/naming-preference* nits. The prompt lives in THREE places that must stay identical — `server/src/db/seed-prompts.ts#GENERAL_REVIEWER_PROMPT`, `docs/agent-prompts/general-reviewer.md` (same text + a trailing newline), and the live `agents.system_prompt` row. `seed.ts` only inserts missing agents, so editing the seed never reaches an existing DB — update the running agent with `PUT /agents/:id` (bumps its version). Security/Performance prompts already had concrete, non-contradictory SUGGESTION definitions.
  Where: `server/src/db/seed-prompts.ts:69` (SUGGESTION definition in `GENERAL_REVIEWER_PROMPT`), `docs/agent-prompts/general-reviewer.md:59`, `server/src/db/seed.ts:220` (inserts only missing agents), `server/src/modules/agents/routes.ts:109` (`PUT /agents/:id`), `reviewer-core/src/review/reduce.ts:16` (`SUGGESTION: 3` penalty)
  **Correction (2026-09-19):** two things. (1) The ban was narrowed only in the Quality bar (`server/src/db/seed-prompts.ts:57`); the Clarity section still says "This is not a license to report style nits" (`seed-prompts.ts:47`) — harmless now that SUGGESTION has concrete cases, but "narrowed" overstates it. (2) The "three copies identical" invariant is currently BROKEN in the dev DB — the live General Reviewer row (v5) has the same wording (the new SUGGESTION rule IS live) but every backtick and every `**bold**` marker is gone: 0 vs 24 backticks and 0 vs 12 `**` in `docs/agent-prompts/general-reviewer.md`, 4592 chars vs 4640 for `GENERAL_REVIEWER_PROMPT` (the `.md` adds a trailing newline → 4641); `#` headings survived (read-only `SELECT` on `devdigest-postgres`). Cause unknown — see Open Questions. Until resolved, re-`PUT` the file's exact text after any prompt change and compare lengths.

## Tool & Library Notes

## Recurring Errors & Fixes

## Session Notes

## Open Questions

- **2026-09-19 · What stripped the markdown from the live General Reviewer prompt?** — The dev-DB `agents.system_prompt` for General Reviewer v5 has no backticks and no `**` at all, while `docs/agent-prompts/general-reviewer.md` has 24 and 12 (4592 vs 4640 chars; headings kept) — the pattern of a markdown-to-plain-text pass, not of shell quoting alone. Checked: the wording otherwise matches, and the server passes the text through untouched (`server/src/modules/agents/service.ts:101` → `server/src/modules/agents/repository.ts:131`). Open: was the prompt pasted through the Agent editor (does `ConfigTab` or its textarea drop code spans?), or sent via a shell `curl` where backticks were eaten by command substitution? Whoever edits the prompt next: `PUT` the file content via `node`/`fetch` (no shell quoting) and compare `length(system_prompt)` with the file.
  Where: `server/src/modules/agents/routes.ts:109` (`PUT /agents/:id`), `docs/agent-prompts/general-reviewer.md:59`, `server/src/db/seed-prompts.ts:11` (`GENERAL_REVIEWER_PROMPT`)

- **2026-09-19 · Cancelling a run is unreliable** — two independent holes: (1) `cancelRun` calls `runBus.cancel()` then `runBus.complete()` after one DB await, and `complete()` clears the cancel flag, so the runner's checkpoint only sees it during that await; (2) `checkCancelled` runs only before each LLM call, and `completeAgentRun` has no status guard, so a runner already inside its last call persists the review and overwrites `cancelled` with `done` — in single-pass, any cancel after the call started. Untested.
  Where: `service.ts:87` / `service.ts:89` (`cancel` then `complete`), `server/src/platform/sse.ts:79` (`cancelled.delete`), `run-executor.ts:210` (checkpoint), `repository/run.repo.ts:176` (update by id only)

- **2026-09-19 · The PR title reaches the prompt outside `<untrusted>`** — `taskLine` interpolates `pull.title` and `pull.author`, and `assemblePrompt` adds `task` without `wrapUntrusted`, while `INJECTION_GUARD` tells the model the PR title sits inside an untrusted block. A crafted PR title is therefore trusted text.
  Where: `helpers.ts:84` (`taskLine`), `reviewer-core/src/prompt.ts:105` (task pushed unwrapped), `reviewer-core/src/prompt.ts:18` (the guard's claim)

- **2026-09-19 · `/runs/:id/{events,trace,cancel}` aren't workspace-scoped** — they call `getContext` but act on the bare run id (`DELETE /runs/:id` is scoped). Harmless with one local workspace; a hole once there are several.
  Where: `routes.ts:52`, `routes.ts:115`, `routes.ts:122`, `repository/run.repo.ts:88` (the scoped delete)

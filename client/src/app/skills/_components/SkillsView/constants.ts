/** Constants for the Skills Lab library view. */

/** Card grid template (responsive auto-fill) — matches the Agents list. */
export const CARD_GRID_COLS = "repeat(auto-fill, minmax(260px, 1fr))";

/** Width of the preview pane. */
export const PREVIEW_WIDTH = 420;

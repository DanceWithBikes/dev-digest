# Insights — whole project

Knowledge you can't see in the code. Newest entry on top of each section.
Cross-package insights live here; module-specific ones go to the nearest `docs/insights.md`.
Format and rules: `.claude/skills/engineering-insights/SKILL.md`.
When an insight becomes a permanent rule, move it as one line into the relevant `AGENTS.md` and keep the "why" here.

## What Works

## What Doesn't Work

- **2026-09-21 · A git worktree with symlinked `node_modules` makes `pnpm arch:check` report baselined violations as new** — Tried: building a throwaway review fixture with `git worktree add /tmp/<x>` and `ln -s <main>/server/node_modules` to skip the install. Failed because: dependency-cruiser resolves `drizzle-orm` through the symlink to `../../../../Users/…/server/node_modules/.pnpm/drizzle-orm@…/index.d.ts`, while the baseline stores `node_modules/.pnpm/drizzle-orm@…/index.d.ts`; `--ignore-known` matches on the literal `to` path, so 5 known drizzle edges surfaced as new errors (7 reported instead of the 2 really seeded). Instead: run a real install in the worktree (`cd server && pnpm install --frozen-lockfile --prefer-offline`, ~10 s from the store), and add `node_modules` to `$(git rev-parse --git-common-dir)/info/exclude` if you do symlink anything — the `.gitignore` pattern `node_modules/` has a trailing slash and does not match a symlink, so it shows up as an untracked file.
  Where: `server/.dependency-cruiser.cjs:40` (`only-repositories-touch-the-database`), `server/.dependency-cruiser-known-violations.json` (entries whose `to` starts with `node_modules/.pnpm/`)

- **2026-09-18 · A freshly seeded stack can't demonstrate anything run-linked** — Tried: verifying timeline/trace UI on the hermetic stack (`scripts/e2e.sh`, PG 5433 / API 3101 / web 3100), assuming "seeded demo data" covered the PR page. Failed because: `db/seed.ts` inserts `reviews` + `findings` but NO `agent_runs`, and its review has `run_id = NULL`. So a fresh DB shows the "Review runs" accordion (reviews exist) while the Timeline has no run tiles and no trace drawer is reachable — I handed the user a checklist item that could not pass. Instead: for run-linked UI (timeline tiles, trace drawer, cost badges) either execute a real review first, or assert against a stack where runs exist; the accordion/findings UI is the only part a bare seed can show.
  Where: `server/src/db/seed.ts:137` (`.insert(t.reviews)` — no `runId`; `seed()` never inserts `agentRuns`), `scripts/e2e.sh:128` (`pnpm db:seed`), `client/src/app/repos/[repoId]/pulls/[number]/_components/FindingsTab/FindingsTab.tsx:150` (Timeline renders only with runs or commits — the seed's one commit shows it, but with no run tiles)

- **2026-09-17 · Reference commit 93119a5 added a `run` prop to VerdictBanner but never wired it** — Tried: reusing `93119a5`'s (the pre-revert "run cost badge" commit) `ReviewRunAccordion`/`VerdictBanner` diff verbatim, which adds an optional `run` prop for the cost+tokens line. Failed because: that commit never updated `FindingsTab.tsx`, the only caller of `ReviewRunAccordion` — it never matched a `ReviewRecord` to its `RunSummary` via `run_id`, so `run` stayed at its `null` default and the verdict banner would always render "—" even with real cost data in the DB. Instead: build `runsById = new Map(prRuns.map(r => [r.run_id, r]))` in `FindingsTab.tsx` (it already receives `prRuns` for the Timeline) and pass `run={review.run_id ? (runsById.get(review.run_id) ?? null) : null}` into `ReviewRunAccordion`.
  Where: `client/src/app/repos/[repoId]/pulls/[number]/_components/FindingsTab/FindingsTab.tsx:84` (`runsById`), `FindingsTab.tsx:189` (`run={…}` into `ReviewRunAccordion`)
  **Correction (2026-09-19):** the premise is false — `git show 93119a5 -- '*FindingsTab.tsx'` shows that commit DID wire it: `run={prRuns?.find((r) => r.run_id === review.run_id) ?? null}`. So the verdict banner would have worked; the current `runsById` memo is an O(1) refactor of the same lookup, not a bug fix. Lesson that still stands: before calling a reference commit broken, `git show <sha> -- <caller>` for the caller too.

## Codebase Patterns

- **2026-09-24 · A `docs/specs/` file asserts test coverage per criterion, so writing one in PARALLEL with the tests guarantees it ships stale** — The house spec format makes every acceptance criterion carry its evidence: either `· test: path:line ("the it() name")` or an explicit `· untested` (see `docs/specs/cost-badge.md:11-13`). That is what makes these specs useful, and it also couples them tightly to the test tree — the two L03 specs contain 33 such annotations between them. Running a doc-writing pass concurrently with a test-writing pass therefore produces a document that is wrong on arrival: L03's specs asserted "no integration test exercises the intent routes" and marked four criteria `untested` while `server/test/intent.it.test.ts` was being written in another worktree-less parallel run, and the false claim had to be corrected by hand afterwards. **Sequence docs last** — after the tests land — or accept that every `· untested` mark needs re-verification before the PR. The same coupling applies to `path:line` anchors generally: a spec written beside an in-flight refactor cites lines that have already moved. (Related: the spec format is also why `plan-verifier` can check coverage claims mechanically.)
  Where: `docs/specs/cost-badge.md:11` (the `· test:` evidence convention), `docs/specs/intent-layer.md:81` (the open question that had to be rewritten), `server/src/modules/reviews/docs/specs/intent-layer.md:24` (a criterion whose `untested` mark was false on arrival), `server/test/intent.it.test.ts:154` (the test the spec claimed did not exist)

- **2026-09-24 · Adding an agent silently rots the `Used by` column of `.claude/agents/README.md`, and appending table rows with `Edit` splits the table** — Two traps in the one file that is supposed to be the staleness checklist for the others. (1) That table was written when it was titled "Where planner's and implementer's rules come from", so 7 of its 18 rows said `both` — correct and unambiguous for two agents, and silently meaningless the moment the title was generalised and rows naming `test-writer`/`architecture-reviewer` appeared beside them. Relative shorthands (`both`, `all`, `the other one`) in a table whose row set grows do not survive; write the agent names out, and when you add an agent re-derive `Used by` by grepping each agent file for the rule rather than guessing — e.g. `grep -l 'ln -s AGENTS.md CLAUDE.md' .claude/agents/*.md`. (2) Both tables end with a trailing blank line before the prose paragraph; an `Edit` that anchors on the *last row* and appends after it lands the new rows below that blank, which splits one markdown table into two that render as separate tables with no header. Anchor on the paragraph that follows the table instead, and verify with an `awk` pass that no blank line sits between the header row and the last row.
  Where: `.claude/agents/README.md:109` (`## Where the agents' rules come from` — the renamed heading), `.claude/agents/README.md:114` (the table header the rows must stay contiguous with), `.claude/agents/README.md:83` (`## Inputs and outputs` — the table that was split this way)
  **Addition (2026-09-24):** both halves of this entry were executed when `insights-curator` was added, and each needs one more thing. (1) The `Used by` re-derivation works but over-reports: `grep -l 'CLAUDE.md' .claude/agents/*.md` matches an agent that only names the file as a *forbidden path*, not one that follows the module-map rule — so grep gives you the candidate set, and you still have to open each hit and decide, which the new row records explicitly rather than pretending the match was clean. (2) The contiguity check is `awk 'BEGIN{prev=0} /^\|/{if(prev>0 && NR-prev>1) print "SPLIT before line " NR; prev=NR}' <file>`, and it reports every *legitimate* table boundary too — three tables in this README means two expected SPLITs. It is only a failure when the count exceeds (tables − 1), so pair it with a per-table row count: `awk '/^\| Agent \| Responsibility/{t="set"} /^\| Agent \| Consumes/{t="io"} /^\| Rule in the prompt/{t="rules"} /^\|/{c[t]++} END{for(k in c) print k, c[k]-2}'` must print the agent count for `set` and `io`.

- **2026-09-21 · A `pr-self-review` re-run must re-verify EVERY carried finding, and insights must be written BEFORE the verdict** — Two traps of the incremental re-run, both hit in one session. (1) `changedSince` is relative to the LAST verdict only, and the plan surfaces just `openCriticals` for re-checking, so non-critical findings ride along unexamined: the previous verdict still listed "hook matches the command inside heredocs" and "hook fails open" against `.claude/hooks/pr-self-review-gate.mjs` although that file (same hash in both verdicts) already contained the fix — a carried finding can be stale even when its file is NOT in `changedSince`. On a re-run, open each carried finding's `file:line` and confirm it still holds (for the hook: pipe a synthetic PreToolUse payload to it) before copying it into the new verdict. (2) The `Stop` hook asks for insights AFTER the report, but `docs/insights.md` is itself part of the branch diff, so writing the insight changes the fingerprint and turns a fresh PASS into `stale`. Write insights before `record-verdict.mjs`, or re-run the gates and re-record afterwards — never leave the user with a PASS that the next edit silently invalidated.
  Where: `.claude/skills/pr-self-review/scripts/collect-changes.mjs:214` (`openCriticals` — the only findings the plan re-surfaces), `.claude/skills/pr-self-review/SKILL.md:72` ("On a re-run"), `.claude/skills/pr-self-review/scripts/lib.mjs:61` (`fingerprint`), `.claude/settings.json:20` (`insights-reminder.mjs` on `Stop`)

- **2026-09-21 · In this codebase "token" means LLM tokens, not credentials — never use it as a security keyword** — The first content trigger that routes client files to the `security` skill in `pr-self-review` included a bare `token`, and it matched every cost/usage component (`tokensIn`, `tokensOut`, `formatTokens`), sending pure display code to a security pass that had nothing to say. The same trap applies to any grep-based secret scan or lint rule written for this repo: match `accessToken|authToken|Authorization|Bearer |apiKey|api_key|secret|password`, not `token`.
  Where: `.claude/skills/pr-self-review/skill-routing.json:102` (`security#client-by-content` → `contains`), `client/src/components/run-cost-badge/RunCostBadge.tsx:30` (`tokensIn`)

- **2026-09-18 · Every CI workflow is path-filtered, so file placement decides whether CI runs at all** — All five `.github/workflows/*.yml` trigger on `pull_request`, but each scopes itself with `paths:` — `client.yml` → `client/**`; `server-unit.yml` → `server/**`, `reviewer-core/**`; `server-integration.yml` → `server/**`; `reviewer-core.yml` → `reviewer-core/**`, `server/src/vendor/shared/**`; `e2e-web.yml` → `client/**`, `server/**`, `e2e/**`. A file in a NEW top-level directory (e.g. `demo/`) therefore fires zero workflows, sits outside every package's `tsconfig` (so it can't break `pnpm typecheck`), and stays out of dependency-cruiser's graph — the right home for reviewer fixtures or scratch code that must be committable without running the Docker-backed e2e job. Flip side: touching `server/**` in a PR fires three workflows including integration + e2e.
  Where: `pull_request` `paths:` at `.github/workflows/client.yml:14`, `server-unit.yml:24`, `server-integration.yml:20`, `reviewer-core.yml:17`, `e2e-web.yml:20`; `demo/findings-code-example.ts:1`
  **Correction (2026-09-19):** (1) every workflow ALSO runs on `push` to `main` with the same filters (`paths:` at `client.yml:10`, `server-unit.yml:19`, `server-integration.yml:16`, `reviewer-core.yml:12`, `e2e-web.yml:14`), and each list includes its own `.yml`. (2) "stays out of dependency-cruiser's graph" is wrong: there is no repo-level dependency-cruiser config or CI step — it only runs inside repo-intel (`server/src/adapters/depgraph/index.ts:17`) over whatever repo gets indexed, and the walk doesn't exclude `demo/` (`server/src/modules/repo-intel/constants.ts:17`, `EXCLUDED_DIRS`), so indexing this repo in the studio DOES graph `demo/`.

- **2026-09-17 · A reverted commit can be a ready-made reference after a starter-state reset** — This repo was reset to its course starter state (`c6af1e4`), reverting real feature work done on it. `git show <sha> -- <path>` on a commit found via `git log --all --oneline -- <path>` can surface a near-complete, directly reusable diff for a feature that looks like course homework — e.g. `93119a5` ("run cost badge") supplied the schema/migration/contracts/executor/repo/route diff and a `RunCostBadge` component wholesale. Check history before re-implementing from scratch.
  Where: no code anchor — `git show 93119a5` (reset commit `c6af1e4`); its migration was named `0010_polite_sasquatch.sql`, the current one is `server/src/db/migrations/0010_exotic_bloodaxe.sql:1`

- **2026-09-17 · The two shared-contract copies have drifted** — `adapters.ts` and `contracts/{trace,eval-ci,knowledge,productionize}.ts` differ between copies (in `trace.ts` only comments). Run `diff -rq server/src/vendor/shared client/src/vendor/shared` before changing a contract.
  Where: `server/src/vendor/shared/adapters.ts:69` (`sessionId` — server only), `server/src/vendor/shared/adapters.ts:83` (provider `id` union has `'openrouter'`, client's doesn't), `server/src/vendor/shared/contracts/trace.ts:44` (comment-only drift); `contracts/platform.ts` is identical in both copies

- **2026-09-17 · costUsd now persists from the engine to the DB (L01, resolved)** — Added `agent_runs.cost_usd` (migration `0010_exotic_bloodaxe.sql`) and threaded it through `run-executor.ts` → `run.repo.ts` → `repository.ts` → shared contracts (`RunSummary.cost_usd`, `RunStats.cost_usd`, `PrMeta.cost_usd`, both vendor/shared copies) → 4 UI surfaces (PR list COST column, PR-detail Timeline row, VerdictBanner, Run Trace drawer Stats panel). `RunStats` (the Trace drawer's stats, distinct from `RunSummary`) needed its own `cost_usd` — no prior commit had touched it. `null` still means "model price unknown"; UI renders "—", never `$0.00`. CI-sourced `agent_runs` rows (`source='ci'`) are out of scope and still land with `cost_usd = null`.
  Where: `server/src/db/migrations/0010_exotic_bloodaxe.sql:1`, `server/src/modules/reviews/run-executor.ts:248` (`costUsd` into `completeAgentRun`), `server/src/modules/reviews/repository/run.repo.ts:169` (`costUsd: values.costUsd ?? null`), `server/src/vendor/shared/contracts/trace.ts:68` (`RunStats.cost_usd`) / `:108` (`RunSummary.cost_usd`), `client/src/components/run-cost-badge/RunCostBadge.tsx:28` (`RunCostBadge`)
  **Update (2026-09-19):** the PR list COST column now shows the TOTAL of all the PR's runs, not the latest run — see `server/src/modules/pulls/docs/insights.md` (`totalCostByPr`).

## Tool & Library Notes

- **2026-09-23 · Subagent and Skill frontmatter are two different schemas, and a wrong key is silently ignored — plus `skills:` preloads, it does not restrict** — Authoring `.claude/agents/{planner,implementer}.md` surfaced three traps. (1) `allowed-tools` and `disable-model-invocation` are **Skill** (`SKILL.md`) fields; the subagent schema is `name`, `description`, `tools`, `disallowedTools`, `model`, `permissionMode`, `maxTurns`, `skills`, `mcpServers`, `hooks`, `memory`, `background`, `omitClaudeMd`, `effort`, `isolation`, `color`, `initialPrompt`. The two share no key but `model`, and Claude Code ignores an unrecognised field **without reporting an error** — so `allowed-tools:` in an agent file looks like a permission boundary and enforces nothing. (2) The `skills:` list **preloads** those skills' full bodies into the subagent's startup context; it does NOT limit which skills it may invoke. Only `Skill` being absent from `tools` (or listed in `disallowedTools`) blocks skill use — so the trap recorded in the researcher session note below stands: you cannot grant one skill and withhold another via `skills:`. (3) `tools` inherits **everything** when omitted, and a `tools` entry that resolves to no tool makes the agent fail to launch with an error naming it — so an unlisted `tools:` is maximal privilege, not a safe default. Validate frontmatter with the vendored yaml: `node -e "const Y=require('$PWD/server/node_modules/.pnpm/yaml@2.9.0/node_modules/yaml');…"` — there is no root `package.json`, so no parser resolves by bare specifier from the repo root (the `ruby -ryaml` one-liner in the 2026-09-20 entry below works too).
  Where: `.claude/agents/planner.md:6` (`skills:` — preload, not a restriction), `.claude/agents/planner.md:4` (`tools:` — no `Write`/`Edit`), `.claude/agents/implementer.md:5` (`disallowedTools: WebSearch, WebFetch`)
  **Addition (2026-09-23, same day):** a fourth trap, hit while revising both agents — an agent states its mechanism in **two** places, and the body is the one you edit. `description` is not documentation: it is always resident in context and is what the router matches on, so rewriting a `## Step` without rewriting the description leaves the lie in the only field other agents actually read. Both files described themselves as routing skills "via skill-routing.json" for a while after the body had stopped doing that. Checklist when editing any `.claude/agents/*.md`: re-read the description against the new body, and bump `metadata.version` (the skills use `1.1.0` for a revision — `frontend-ui-architecture` is the precedent).

- **2026-09-21 · A PreToolUse hook on `Bash` sees the raw command string and the SESSION cwd — both bite a command-matching gate** — The first version of the PR gate tested `/gh\s+pr\s+create/` against `tool_input.command` and denied three harmless calls in one afternoon: a heredoc writing a report that mentioned the command, a `python3 - <<EOF` script containing it in a string, and `gh pr create --help`. Separately, `input.cwd` is the session's directory, so `cd /tmp/worktree && gh pr create` was judged against the main checkout's verdict instead of the worktree's. What to do in any hook that gates on a command: drop heredoc bodies and quoted strings before matching, require command position (`^` or after `; & | (` / newline, allowing `VAR=x` prefixes), and resolve the effective directory from leading `cd <dir>` segments. Also fail closed: a `catch { return }` around the verification lets the call through on any error, and an uncaught throw exits 1, which Claude Code treats as a non-blocking hook error — i.e. also "allow". Project hooks are live in the session that adds them (no restart needed), so test with a synthetic payload piped to the script before touching `.claude/settings.json`.
  Where: `.claude/hooks/pr-self-review-gate.mjs:9` (`stripText`), `.claude/hooks/pr-self-review-gate.mjs:16` (`LEADING_CD`), `.claude/hooks/pr-self-review-gate.mjs:60` (fail-closed `deny`)

- **2026-09-21 · `server` typecheck needs `reviewer-core/node_modules`, and `reviewer-core` is an npm package** — `server/tsconfig.json` maps `@devdigest/reviewer-core` to `../reviewer-core/src`, so `cd server && pnpm typecheck` compiles that source and fails with 7 × `Cannot find module 'zod'` / `'openai'` when `reviewer-core` has no install — none of the errors are in the diff. `reviewer-core` and `e2e` commit `package-lock.json`: use `npm ci` there. `pnpm install --frozen-lockfile` in `reviewer-core` fails (`ERR_PNPM_NO_LOCKFILE`), and a plain `pnpm install` exits 1 on the build-script gate while leaving a stray `pnpm-lock.yaml` + `pnpm-workspace.yaml` behind, which then show up as untracked files in the diff. The root `AGENTS.md` line "`cd <package> && pnpm test`" is only literally true for `client/` and `server/`.
  Where: `.claude/skills/pr-self-review/skill-routing.json:116` (`packageManagers`), `.claude/skills/pr-self-review/skill-routing.json:122` (`requires` on `server typecheck`)

- **2026-09-21 · Subagents here cannot write report files, and an orchestrating subagent hands back before its background children finish** — Two harness behaviours that shaped the `pr-self-review` evals. (1) A subagent's `Write` of `report.md` is refused with "Subagents should return findings as text, not write report files" (`notes.md`, `findings.json` and logs were accepted); ask eval subagents to return the report in their final message and save it yourself. (2) A subagent that launches reviewers with the default background mode is forced to report while they are still running: the run on this branch recorded its verdict with 2 of 5 skill passes missing, and the architecture findings arrived minutes later. Any skill that fans out and then aggregates must launch its children with `run_in_background: false`, and its script should distinguish "review unfinished" from "code is bad" (here: `INCOMPLETE` vs `BLOCKED`).
  Where: no code anchor for the harness behaviour — `.claude/skills/pr-self-review/SKILL.md` (step 3, "run them in the foreground"), `.claude/skills/pr-self-review/scripts/record-verdict.mjs:99` (`INCOMPLETE`)

- **2026-09-21 · skill-creator's description optimizer reports ~10% recall for a good description unless you work around three things** — Tried: `python -m scripts.run_loop …` as the `skill-creator` skill documents, for `frontend-ui-architecture`. Failed because: (1) the machine has only Python 3.9.6 and the scripts use `X | None` annotations (`TypeError: unsupported operand type(s) for |`) — copy `scripts/` and `eval-viewer/generate_review.py` to `/tmp` and prepend `from __future__ import annotations` to each file (the synced skill dir is not ours to edit); (2) `run_eval.py` registers a temp command `<skill>-skill-<uuid>` per worker in `<project>/.claude/commands/` and counts a trigger only when the model calls THAT uuid — with the default 10 parallel workers the model sees ten identically described commands and picks any of them, so a description that triggers 4/4 times by hand scored recall 11%; (3) run from inside this repo, the real skill competes with the temp command and wins, scoring as a miss. Instead: match on the `<skill>-skill-` prefix in the `/tmp` copy (or pass `--num-workers 1`), and run with `cwd` set to a scratch project — `/tmp/<x>/.claude/skills/` holding copies of every sibling skill EXCEPT the one under test plus `AGENTS.md` copied as `CLAUDE.md` — using `PYTHONPATH=<scripts copy>` because `find_project_root()` walks up from `cwd`. Launch with `nohup … &`: a 5-iteration loop outlives the 10-minute Bash timeout. After the fix the unchanged description scored 39/39 train, 24/24 held-out. Also seen once: `improve_description.py` died on `claude -p exited 1` with empty stderr — a retry wrapper is cheap insurance.
  Where: no code anchor — `~/.claude/skills/synced/*/skill-creator/scripts/run_eval.py` (`run_single_query`, `clean_name`; `find_project_root`); query set at `.claude/skills/frontend-ui-architecture/evals/trigger-evals.json:1`

- **2026-09-20 · A control ("without the skill") eval run inside this repo is not a control unless you forbid the skill explicitly** — Project skills are auto-discovered: `frontend-ui-architecture` appeared in the Skill tool's list seconds after its `SKILL.md` was written, with no restart, and every subagent spawned in this repo inherits that list. So a baseline subagent given only the task prompt can still load the skill under test and the with/without comparison silently measures nothing. When evaluating a skill that already lives in `.claude/skills/`, put both constraints in the baseline prompt — "do NOT use the Skill tool" and "do NOT read anything under `.claude/skills/<name>/`" — and keep the eval workspace outside the repo (`/tmp/<skill>-workspace/`) so outputs are neither committed nor picked up as project files. Also: a skill's version belongs in frontmatter `metadata.version` as a quoted string (the Agent Skills spec allows only a string→string map there; a top-level `version:` key is not in the spec), and `name` must equal the directory name. No `pyyaml` on this machine — validate with `ruby -ryaml -e 'p YAML.load(File.read("SKILL.md").split("---")[1])'`.
  **Update (2026-09-21):** the same auto-discovery makes the POSITIVE test free — a subagent spawned minutes after `backend-onion-architecture/SKILL.md` was written invoked it unprompted, before drafting, so triggering needs no restart and no priming, just a realistic task prompt. Worth doing even when you are confident in the description: that run scored 7/7 on the eval cases AND caught a factual error in the skill *body* (it claimed the baseline held "43 violations across six rules"; a rule added later made it seven). Lesson beyond the typo — never hardcode a number derived from a generated artifact into skill prose; state the distribution, cite the artifact, or have the reader run `jq`. Evals validate content, not only the description.
  Where: `.claude/skills/frontend-ui-architecture/SKILL.md:4` (`metadata`), `.claude/skills/frontend-ui-architecture/evals/evals.json:1`; no code anchor for skill auto-discovery — harness behaviour

- **2026-09-20 · `skills-lock.json` does not describe `.claude/skills/` — don't use it as the skill inventory** — The lockfile tracks 8 skills, the directory holds 12, and they disagree both ways: `architecture-patterns` and `github-workflow-automation` are locked but have no directory; `engineering-insights`, `frontend-ui-architecture`, `mermaid-diagram`, `react-best-practices`, `react-testing-library` and `security` exist but are not locked. Lock entries carry a `source` repo + `computedHash`, so only skills installed from GitHub belong there; a hand-written project skill is just a directory with a `SKILL.md` and needs no lock entry (don't invent a hash). The catalog in `.claude/skills/README.md` is the human-facing list to update instead — though its claim of a `.cursor/skills/` symlink is stale: there is no `.cursor` directory.
  Where: `skills-lock.json:4` (`architecture-patterns` — no directory), `skills-lock.json:22` (`github-workflow-automation` — no directory), `.claude/skills/README.md:3` (the stale `.cursor/skills/` symlink claim)

- **2026-09-20 · You cannot verify a memory-file change from the session that made it** — Claude Code loads `CLAUDE.md`/`AGENTS.md` at session start, so after editing one the running session still holds the OLD text in context: every in-session "check" only re-reads the file from disk and proves nothing about what the agent was primed with. Don't report memory changes as "verified" from the same session. Real test: a NEW session + a canary — add `- Canary: the magic word is <word>.` to the target `AGENTS.md`, open a file in that directory (nested memory loads on touch, not at startup), ask for the magic word, then remove the line. What IS verifiable in-session: symlink integrity (`find . -type l ! -exec test -e {} \; -print` → empty; no `CLAUDE.md` of `-type f`) and write-through, non-destructively, by cloning to `/tmp`, appending through `CLAUDE.md` with `node -e "require('fs').appendFileSync(…)"`, and confirming `git status` reports `M AGENTS.md`.
  Where: no code anchor — Claude Code session start / nested memory load; closest: `AGENTS.md:25` (the symlink rule), `client/AGENTS.md:1` (a nested file to canary)
  **Correction (2026-09-20):** "a NEW session" is only required for the ROOT `AGENTS.md` (and for directories the session already touched). A NESTED memory file in a directory not yet touched this session loads FRESH from disk — canary verified in one session: appended `- Canary: the magic word is PLATYPUS-7719.` through `server/src/modules/polling/CLAUDE.md`, then read `polling/routes.ts`, and the canary came back in the injected `Contents of …/polling/CLAUDE.md` block. So the write-through test needs no `/tmp` clone either: append in place through `CLAUDE.md`, check `git status` says `M AGENTS.md`, touch a file in that directory to confirm the load, then `git checkout --` the memory file. Two more things this proved: the harness still injects the block under the `CLAUDE.md` name (the bytes come from `AGENTS.md`), and the `[repoId]/[number]` bracket path — which breaks zsh loops, see the next entry — loads without trouble.

- **2026-09-20 · Iterating the repo's memory files in zsh needs `while read`, never `for f in $(git ls-files …)`** — one memory file lives at `client/src/app/repos/[repoId]/pulls/[number]/`, and unquoted command substitution undergoes pathname expansion: zsh treats `[repoId]` as a character-class glob, matches nothing, and aborts the whole command with `no matches found` (bash silently passes it through, so a script tested elsewhere breaks only here). Use `git ls-files '*AGENTS.md' | while IFS= read -r f; do … "$f"; done` — the quoted `"$f"` never re-globs. Same trap for any `find`/`ls` loop over `client/src/app/**`.
  Where: `client/src/app/repos/[repoId]/pulls/[number]/AGENTS.md:1` (the bracket path), `AGENTS.md:25` (the rule that sends you looping over these files)

- **2026-09-20 · The committed `CLAUDE.md` symlinks: what git and the editing tools actually do** — Each `CLAUDE.md` is a git object of mode `120000` whose content is the literal string `AGENTS.md` (relative, bare sibling name — never absolute, or it breaks in worktrees/containers). Consequences worth knowing before touching them: `git status` reports the conversion as `T` (typechange), **not** a rename, so `--find-renames` shows 18 adds + 18 typechanges; `git clone` restores real symlinks (verified), except on Windows without developer mode / `core.symlinks=true`, where a clone writes plain text files containing `AGENTS.md` — accepted, the stack is macOS/Linux; and because `fs.writeFile` follows symlinks, editing `AGENTS.md` fires a "`CLAUDE.md` changed on disk" notice in the same turn — that is the link working, not a conflict, so don't "restore" anything. To verify the set: `git ls-files -s | awk '$1=="120000"' | wc -l` → 18.
  Where: `AGENTS.md:25` (the convention rule), `.gitignore:20` (`clones/` — why `server/clones/**` memory files are out of scope)
  **Correction (2026-09-24):** the count is now **19**, not 18 — a module gained its memory pair after that entry was written. Don't use `→ 18` as a pass/fail invariant; it is a drifting number, so re-derive it with `git ls-files -s | awk '$1=="120000"' | wc -l` and compare against `find . -name AGENTS.md -not -path '*/node_modules/*' -not -path './.claude/*'` instead. The one real invariant: every `AGENTS.md` outside `.claude/skills/` has a `CLAUDE.md` symlink beside it — except `server/src/modules/skills/`, which has `docs/` but no `AGENTS.md` at all and therefore no symlink.

- **2026-09-19 · `gh` is installed but may be unauthenticated — check `gh auth status` before `gh pr create`** — `gh` was missing (`command not found: gh`) and was installed with `brew install gh` (2.101.0); its first device-flow login ended in `access_denied`, so it may still be logged out. `gh auth login` is interactive, but `echo | gh auth login --web --hostname github.com --git-protocol ssh --skip-ssh-key` run as a background Bash task prints the one-time code + `https://github.com/login/device` to its output file and exits once the user approves or denies. If auth isn't available, don't block: print `https://github.com/DanceWithBikes/dev-digest/compare/main...<branch>?expand=1&title=<enc>&body=<enc>` built with `encodeURIComponent` in a `node -e` one-liner — GitHub opens the PR form fully filled in.
  Where: no code anchor — `gh auth status` (still logged out on 2026-09-19); remote `origin` = `git@github.com:DanceWithBikes/dev-digest.git`

- **2026-09-19 · Syntax-check a file outside every tsconfig with `typescript.transpileModule`, not esbuild** — Files in `demo/` belong to no package, so `pnpm typecheck` never sees them, and their imports (e.g. `./schema`) may not exist on purpose. `require('esbuild')` from `server/` fails with `Cannot find module 'esbuild'` (it's only a nested dependency of `tsx`, not hoisted). What works from `server/`: `node -e "const ts=require('typescript');const r=ts.transpileModule(require('fs').readFileSync('../demo/<file>.ts','utf8'),{reportDiagnostics:true,compilerOptions:{module:99,target:99}});console.log(r.diagnostics.length?r.diagnostics:'syntax OK')"` — syntax only, unresolved imports are ignored.
  Where: no code anchor — closest: `server/package.json:47` (`typescript`, the one you `require`), `demo/credits-ledger.ts:12` (a deliberately unresolvable `./schema` import)

- **2026-09-18 · `tsx -e` compiles as CommonJS — top-level `await` fails** — Running a one-off script with `pnpm exec tsx -e '…await fetch(…)'` in `server/` dies with `Top-level await is currently not supported with the "cjs" output format`, and the request never goes out (easy to miss when the output is tailed). Wrap the body in `(async () => { … })();`, or write a temp `.mts` file. Useful pattern this came from: importing a TS constant straight from source (e.g. `GENERAL_REVIEWER_PROMPT`) to PUT it to the API guarantees the live value is byte-identical to the file.
  Where: no code anchor — closest: `server/package.json:46` (`tsx`); example constant `server/src/db/seed-prompts.ts:11` (`GENERAL_REVIEWER_PROMPT`)

- **2026-09-18 · The "DevDigest Design (standalone) .html" exports are self-extracting JSX bundles, not plain markup** — These files (shared by the user as design references, e.g. `~/Downloads/DevDigest Design (standalone) (1).html`) are a `__bundler_manifest`/`__bundler_template`-driven loader: ~180 lines of JS that unpacks a `<script type="__bundler/manifest">` JSON map of `{mime, compressed, data}` entries (gzip+base64) client-side via `fflate` (this is why `fflate` is a dependency per an earlier commit). Reading the file directly (Read tool, grep) shows only base64 noise. To inspect the actual design source: extract the manifest JSON with a regex/Node script, then per entry `zlib.gunzipSync(Buffer.from(entry.data, "base64"))`. The decompressed entries are real, readable `.jsx` files — one per screen (`screen_dashboard.jsx`, `screen_pr_detail.jsx`, `prdetail_runs.jsx`, `findings.jsx`, `primitives.jsx` with the `SEV` severity tokens, `data.jsx`/`data2.jsx` mock data, etc.) plus vendored React/React-DOM and woff2 fonts — this is a far more precise design source than the equivalent screenshots (exact JSX structure, style tokens, copy), and worth decoding whenever the user shares one of these exports instead of relying on screenshots alone.
  Where: no code anchor — external `*.html` design exports (e.g. in `~/Downloads`); decode via Node `zlib`/`Buffer`
  **Correction (2026-09-19):** "this is why `fflate` is a dependency" is false — `fflate` is in no `package.json` today. It was added in `2006964` for the (since reverted) skills module, unrelated to these exports, and removed by the `c6af1e4` reset. Decoding the exports needs no project dependency — Node's built-in `zlib` is enough.

- **2026-09-17 · `pnpm db:generate`/`pnpm typecheck` can block on pnpm's build-script approval gate** — First run in `server/` or `client/` this session failed with `ERR_PNPM_IGNORED_BUILDS` (pnpm 10's supply-chain policy blocks native postinstall scripts — esbuild, sharp, ssh2, cpu-features, protobufjs — until approved). Fix: `pnpm approve-builds --all -y` in the affected package dir, then re-run the command. Side effect: this writes a new `<package>/pnpm-workspace.yaml` with an `allowBuilds:` map to persist the approval — expect it as an untracked file, it isn't a workspace declaration and doesn't contradict the "no workspace, per-package lockfiles" rule in the root `AGENTS.md`.
  Where: `server/pnpm-workspace.yaml:1` (`allowBuilds:` — cpu-features, esbuild, protobufjs, ssh2), `client/pnpm-workspace.yaml:1` (esbuild, sharp); the no-workspace rule is `AGENTS.md:26`
  **Correction (2026-09-19):** "expect it as an untracked file" is stale — both `pnpm-workspace.yaml` files are committed now (`git ls-files`), so a fresh checkout already has the approvals and shouldn't hit the gate.

- **2026-09-17 · Bulk shell overwrites of docs are denied in auto mode** — a `for … sed > "$f"` loop rewriting all `docs/insights.md` files was blocked by the auto-mode classifier as "irreversible local destruction". Instead: `Read` each file, then `Edit` it (parallel calls are fine).
  Where: no code anchor — auto-mode permission classifier; applies to any bulk rewrite of `**/docs/insights.md` (13 files)

## Recurring Errors & Fixes

- **2026-09-23 · A nested ``` example inside a ```markdown block silently truncates the rest of the file's rendering** — Bit us in `.claude/agents/researcher.md`: a report template wrapped in ` ```markdown ` contained an evidence example wrapped in ` ```ts `. Per CommonMark a closing fence may be indented up to 3 spaces and may NOT carry an info string, so the inner ` ```ts ` does not close anything, but the inner bare ` ``` ` closes the *outer* block. Everything after it renders as live markdown (template `## Headings` become real headings, and placeholders like `<what would raise it>` vanish — parsed as HTML tags), and the intended outer closing fence re-opens a block that swallows the next section whole. The file looks fine in the editor's left pane and wrong only in preview. Fix: give the outer fence more backticks than any fence it contains — ` ````markdown ` … ` ```` ` — the inner three-backtick example then nests safely. Applies to every `SKILL.md`/`AGENTS.md` here that shows a template containing code. Quick check: `awk '/^````/{f4=!f4;next} /^```/&&!f4{f3=!f3;next} END{print f4, f3}' <file>` must print `0 0`.
  Second defect of the same class, which that awk check does NOT catch: a fence placed at column 0 inside a numbered list silently splits the list in two (parser showed `ol start=1, 4 items` → code → `ol start=5, 1 item`); numbering only survives because the second list inherits `start=5`. Indent the fence to the item's content column (3 spaces after a `4. ` marker) to keep it nested. Don't eyeball either problem — parse the tree. A real CommonMark+GFM parser is already vendored: `node -e` with `mdast-util-from-markdown` + `micromark-extension-gfm` resolved by absolute path out of `client/node_modules/.pnpm/…` (they are transitive deps of `react-markdown`, so they are NOT resolvable by bare specifier from outside `client/`), then print `tree.children` types — any `html` node or an unexpected heading means a template leaked out of its code block.
  Third variant of the same class (2026-09-23), caught while writing `.claude/agents/planner.md` and caught by NEITHER the awk check nor the mdast top-level scan: inline code that itself contains backticks. Writing the acceptance-criteria format as `` `- [ ] <behaviour> — `path` (`symbol`) …` `` renders as code-`path`-code-`symbol`, i.e. the delimiters land in the wrong places and the literal template is lost. A single-backtick span cannot contain a backtick — delimit with **two** backticks plus a padding space on each side, exactly as `docs/specs/README.md:16` already does. Rule of thumb for these three variants: when writing a template that shows code, copy the delimiter style from a file in this repo that already shows the same template, rather than inventing it.
  Where: `.claude/agents/researcher.md:62` (the fence indented back into list item 4 — the surviving example), `docs/specs/README.md:16` (the correct double-backtick form), `.claude/agents/planner.md:105` (the fixed line)
  **Correction (2026-09-23, same day):** the four-backtick anchors this entry first cited (`researcher.md:84`/`:93`/`:114`) no longer exist. The nesting was only needed because the report templates were literal ` ```markdown ` blocks; on review those read as grey walls of source in any preview, so they were rewritten as live markdown (an ordered list naming each section heading in inline code) and the file shrank 187 → 132 lines. The quirk itself is unchanged and still bites any `SKILL.md` that shows a template containing code — but prefer describing the template in live markdown over embedding it, and reach for four backticks only when the literal source really must be copy-pasteable.

## Session Notes

- **2026-09-24 · `insights-curator` — the first agent allowed to edit `docs/insights.md`, and the carve-out that made it legal** — Added the eighth subagent: it reads all 15 insights files as one set and does the maintenance half of the `engineering-insights` skill that never happens in practice. The skill already specified dedupe, `×3` → `AGENTS.md` promotion, removing an answered Open Question and keeping ~10 Session Notes — but it runs as a side-action inside another task, so the writing agent opens one file, appends one entry and leaves; nobody ever reads the set. Three design decisions worth keeping. (1) **The append-only rule had to be amended, not worked around**: `SKILL.md` said "never delete or reword other entries", so the agent would have contradicted its own rulebook — it now carries an explicit carve-out naming the agent and exactly three mechanical operations. Before writing an agent that maintains something, check whether the thing's own protocol forbids maintenance. (2) **`doc-writer` and `insights-curator` have deliberately inverse allowlists** — `doc-writer` may write `docs/**` *except* `docs/insights.md`; the curator may write `docs/insights.md` *and nothing else*. That is what lets a promotion cross from insights into an `AGENTS.md` without either agent editing the other's file: the curator proposes, `doc-writer` applies. (3) **`skills: [engineering-insights]` with no `Skill` tool** gives it the rulebook while making "never author a new insight" structural rather than a promise — same pattern as `architecture-reviewer`. Two-tier authority (apply the mechanical and reversible, propose everything that takes judgement) is the whole safety story; when in doubt the prompt says it is a proposal. Not verified: any actual run — the agent has not been invoked yet.
  Where: `.claude/agents/insights-curator.md:22` (the write allowlist), `.claude/agents/insights-curator.md:4` (`tools:` — no `Write`, no `Skill`), `.claude/skills/engineering-insights/SKILL.md:57` (the carve-out), `.claude/agents/doc-writer.md:20` (the inverse allowlist)

- **2026-09-23 · `.claude/agents/README.md` — the map of the subagent set, and the "rule → source" table that keeps it honest** — Wrote the README for the three agents (planner / implementer / researcher): responsibilities, model, `tools` + denials, and the input/output artifact of each. The part worth maintaining is the last table: every project rule quoted inside `planner.md` and `implementer.md` is mapped to the repo file it was copied from (root + package `AGENTS.md`, both `.dependency-cruiser.cjs`, the four `package.json` script blocks, `docs/specs/README.md`, `docs/insights.md` + the `Stop` hook, `.claude/skills/`, and `pr-self-review-gate.mjs` + `settings.json`). Both prompts are **snapshots**, not readers — nothing resolves those files at runtime — so a renamed script or a new cruiser rule makes them silently stale, and that table is the checklist for what to re-read. Deliberate non-goal: the README does not restate any agent's rules or report format beyond its section names; the file is the territory. Convention for the next agent added here: add a row to the three tables, don't paste the prompt.
  Where: `.claude/agents/README.md:1`, `.claude/agents/README.md` ("Where planner's and implementer's rules come from" — the source table), `.claude/skills/README.md:23` (the same map-not-territory pattern for skills)

- **2026-09-23 · `planner` + `implementer` subagents, wired to each other through `skill-routing.json`** — Added `.claude/agents/planner.md` (read-only, opus, preloads the two architecture skills) and `.claude/agents/implementer.md` (Write/Edit, sonnet, `disallowedTools: WebSearch, WebFetch`), completing `researcher → planner → implementer → pr-self-review`. The design decision worth keeping: neither agent carries its own copy of the project's rules — both resolve `file → skill` and `file → gate` from `.claude/skills/pr-self-review/skill-routing.json`, so the plan's "Skills the implementer must apply" and the report's "Skills applied" come from one source and a plan cannot mandate what the implementation rules forbid. That file was written for the PR gate but is really the repo's routing table; treat it as the contract, and remember a new skill absent from it is reported as `unmappedSkills`. Deliberately out of scope for both: architecture and security verdicts (separate reviewers, fresh context on the diff). Not verified — that either agent loads or behaves as specified: `.claude/agents/` is read at session start, so a new session is needed; the behavioural checks (planner refusing to write, implementer staying in scope) are still open.
  **Correction (2026-09-24):** "a new session is needed" is wrong for *registration*. Writing `.claude/agents/{test-writer,architecture-reviewer,plan-verifier,doc-writer}.md` made all four appear as Agent-tool types **in the same session**, minutes after the files were written and with no restart — the harness announced them and listed each one's `tools:` correctly. So agent files are picked up the way skills are (see the 2026-09-20 auto-discovery entry), not the way `AGENTS.md` memory is. What still needs a new session is anything about the *startup context*: whether a preloaded `skills:` body actually reaches an agent that has no `Skill` tool, and whether the agent behaves as its prompt says. Split the claim accordingly — registration is verifiable in-session, priming is not.
  Where: `.claude/agents/planner.md:4` (`tools:` — no `Write`/`Edit`), `.claude/agents/implementer.md:4` (`tools:` — with `Skill`, which Step 1 depends on), `.claude/skills/pr-self-review/skill-routing.json:17` (`skills` — the routing both agents read), `skill-routing.json:118` (`gates` — the shared verification list)
  **Correction (2026-09-23, same day):** the `file → skill` half was wrong, and worth keeping as the lesson — a machine-readable mapping is tempting precisely because it looks more rigorous than judgement. `skill-routing.json` answers "which lens **reviews** this file" (its own `$comment`), not "which skill should **write** it": `notForReview` literally reads "authoring workflow … not a review lens" for `engineering-insights` and `mermaid-diagram`, and `**/*.md` routes to `docs` (prose, no skill). Using it as the selector made authoring skills unreachable, blinded both agents to any skill not registered there, and made `planner.md` contradict itself — Step 2 mandates an `AGENTS.md`/specs step for a new module, Step 3 turned that step into an open question for resolving to zero skills. It also duplicated the built-in mechanism with a narrower copy: skill descriptions are always in context, which is their documented purpose. Now: skills are picked natively by description, and routing is a **floor** that can only add, never shorten. What has no description-based equivalent and stays hard-referenced: `gates` (command + `cwd` + triggering globs) and `packageManagers`. The planner/implementer agreement rests on the gate list, not the skill list. General rule for this repo: before reusing a config as a contract, read what it says it is for — this one says "review" in its first six words.
  **Update (2026-09-23, same day):** the "floor" compromise did not survive either — on the user's call, every reference to `skill-routing.json` is now out of both agents. Reason: it is **hand-maintained**, not generated (`collect-changes.mjs` only consumes it; `.claude/skills/README.md` tells you to register new skills there by hand), so it can silently go stale, and it has not been reviewed as a shared contract. Making two agents depend on an unreviewed internal of a third skill buys determinism with coupling. What replaced it: skills are picked by description alone, and the gate list is written out as a literal table in **both** agents (`planner.md` Step 3 and `implementer.md` Step 3), with each package's `package.json` named as the source of truth when they disagree. That duplicates 8 rows across two files — accepted deliberately, because the rows are small and stable while the coupling was not. If they ever drift, the check is cheap: parse both tables and diff the first two columns.
  Where: `.claude/agents/planner.md:83` (`| Command | cwd | Triggered by |`), `.claude/agents/implementer.md:79` (`| Command | cwd | When your diff touches |` — the same 8 rows), `.claude/skills/README.md:23` ("Adding a review skill? Register it in …" — the manual step that shows the file is hand-maintained)

- **2026-09-23 · First subagent in the repo: `.claude/agents/researcher.md`** — The project had `.claude/skills/`, `.claude/hooks/` and `docs/agent-prompts/` but no `.claude/agents/` directory at all, and no `AGENTS.md` rule about subagents — this is the first one, so there was no local convention to copy (frontmatter follows the skills' style: `name`/`description`/`metadata.version`/`updated`). A read-only researcher with two modes (repo / external) and a clarify-first gate. Implicit convention worth keeping: its restrictions are enforced by the `tools:` allowlist, not by prose — `Write`/`Edit`/`NotebookEdit` are simply absent, and `Skill` is omitted **on purpose** because that is what makes the "no `/deep-research`" rule structural. Trap for whoever edits this file next: adding `Skill` back (e.g. to let the agent use `engineering-insights` and write its own insights) would silently re-enable `/deep-research` too — the allowlist has no per-skill granularity, so keep insight-writing with the calling agent instead. Not verified in-session: that the omission actually blocks the slash command at runtime (it is the documented meaning of the allowlist, but the agent was not invoked); a new session is needed before the file is picked up at all.
  Where: `.claude/agents/researcher.md:4` (the `tools:` line — no `Write`/`Edit`/`Skill`), `.claude/agents/researcher.md:5` (`model: sonnet`), `AGENTS.md:30` (`engineering-insights` rule the agent defers to rather than performing)

- **2026-09-21 · `pr-self-review` skill + the PR gate hook** — Wrote `.claude/skills/pr-self-review/` (SKILL.md, `skill-routing.json`, 3 scripts, 2 references, 3 evals) and `.claude/hooks/pr-self-review-gate.mjs` (PreToolUse on Bash, wired in `.claude/settings.json`). The skill owns no review rules: `collect-changes.mjs` maps every local change (commits since the merge-base + staged + unstaged + untracked) to the project skills by path/content, the agent runs the passes + `arch:check`/`typecheck`, and `record-verdict.mjs` writes PASS / BLOCKED / INCOMPLETE into the git dir keyed by a content fingerprint, which the hook checks before `gh pr create` / `gh pr merge`. Evals (seeded violations, clean change, this branch): the plain agent already finds seeded bugs, so the skill's measurable value is the recorded verdict + hook, rule attribution, and consistent severity - not detection. Iteration 1 fixed: hook false positives, early hand-back, uncommitted-work blindness, fan-out cost. Not done: description-trigger optimisation; a server-side required status check (the hook only stops the agent, not a human).

- **2026-09-21 · `backend-onion-architecture` skill + the backend import-boundary gate** — Wrote `.claude/skills/backend-onion-architecture/` (SKILL.md + 8 references + README with sources + 4 evals) and shipped the enforcement it depends on: `server/.dependency-cruiser.cjs` (13 rules), `arch:check`/`arch:all`/`arch:baseline`, a CI step, and a 43-entry baseline. The skill maps the rings onto the EXISTING file roles (`routes/service/repository` + new `domain.ts`/`ports.ts`/`compose.ts`) instead of introducing `domain/ application/ infrastructure/` folders — the dependency rule is what the gate can check, folder names are not. Also closed the "tiny modules may live in a single `routes.ts`" loophole in `server/src/modules/AGENTS.md`. Not done on purpose: paying down any of the 43 baselined violations (separate task; order is in `references/refactoring-playbook.md`). Follow-up found: `client/.dependency-cruiser.cjs` exists and documents `pnpm arch:check`, but `client/package.json` has the `dependency-cruiser` dep and NO `arch:*` scripts — the client gate is currently unrunnable and has no CI step.
  Where: `.claude/skills/backend-onion-architecture/SKILL.md:1`, `server/.dependency-cruiser.cjs:26` (`forbidden`), `server/src/modules/AGENTS.md:8` (the rewritten layering section), `client/package.json:33` (`dependency-cruiser` with no script to run it)
  **Correction (2026-09-21):** the follow-up was resolved on the same branch — `client/package.json:10` (`arch:check`) exists, `.github/workflows/client.yml:53` runs it, and the gate passes (259 modules cruised); the dep itself sits at `client/package.json:34`.

- **2026-09-20 · CLAUDE.md → AGENTS.md across the repo** — Renamed all 18 tracked memory files to `AGENTS.md` (`git mv`) and put a committed `CLAUDE.md -> AGENTS.md` symlink next to each, so Claude Code's nested auto-load keeps working off a single source of truth while any other agent reads `AGENTS.md`. Also rewrote every path reference (root/`server`/`server/src/modules` memory files, the `engineering-insights` skill, the `Where:` pointers in 4 `docs/insights.md`). Out of scope on purpose: `.claude/skills/zod/AGENTS.md` (a compiled skill guide, not memory — no symlink beside it) and gitignored `server/clones/**`. One commit, `8ebf3ed`.
  Where: `AGENTS.md:25` (the new convention rule), `.claude/skills/engineering-insights/SKILL.md:51` (`AGENTS.md but no docs/insights.md`)

- **2026-09-18 · Findings severity counters shipped** — Severity rollup end-to-end: `PrMeta.severity_counts`/`finding_previews` on the list endpoint, a shared `client/src/components/findings-summary/` (SeverityCounts / SeverityPills / FindingsPopover), pills+filter in `FindingsPanel`, severity icons on timeline tiles, findings in the trace drawer, and "Dismiss" → "Reject" copy. Server 106 unit + 29 integration, client 72 tests. Verified against a hermetic stack (`scripts/e2e.sh` ports: PG 5433 / API 3101 / web 3100) because the dev DB's newest reviews for PR #482 are empty, which hides the feature. Follow-up left alone: each expanded `FindingsPanel` registers its own window `keydown`, so j/k/a/d fire in every open accordion at once.
  Where: `server/src/modules/pulls/routes.ts:194` (`severity_counts`), `client/src/app/repos/[repoId]/pulls/[number]/_components/FindingsPanel/FindingsPanel.tsx:71` (`SeverityPills`), `FindingsPanel.tsx:64` (the per-panel `window` keydown — follow-up still open), `client/src/app/repos/[repoId]/pulls/[number]/_components/RunHistory/RunHistory.tsx:209` (timeline `SeverityCounts`), `client/messages/en/prReview.json:19` (`"Reject"`)
  **Correction (2026-09-19):** the "PR #482 reviews are empty" remark has a concrete cause — seeded #482 has no diff, so every review of it is `0/0 passed` (see `server/src/modules/reviews/docs/insights.md`, deepseek entry).

- **2026-09-17 · Cost badge feature (L01) shipped** — Persisted and surfaced per-run USD cost in 4 places: PR list COST column, PR-detail Timeline, VerdictBanner, Run Trace drawer Stats panel. Reused reverted commit `93119a5` for 3/4 UI surfaces plus the server plumbing; net-new work was `RunStats.cost_usd` for the Trace drawer and the `FindingsTab.tsx` run-matching fix (see What Doesn't Work). Server unit + integration tests and client tests all pass; both packages typecheck clean. Follow-up: CI-sourced runs still get `cost_usd = null` (untouched, out of scope).
  Where: `client/src/app/repos/[repoId]/pulls/[number]/_components/RunTraceDrawer/_components/TraceBody/TraceBody.tsx:70` (Stats cost), `server/src/vendor/shared/contracts/trace.ts:68` (`RunStats.cost_usd`), `client/src/app/repos/[repoId]/pulls/[number]/_components/FindingsTab/FindingsTab.tsx:189` (`run` prop)
  **Correction (2026-09-19):** the "`FindingsTab.tsx` run-matching fix" was not a fix — `93119a5` already wired it (see the corrected What Doesn't Work entry above).

## Open Questions

- **2026-09-24 · Can a subagent here actually ask the user anything, or does "Step 0 — clarify" only ever return the question as its report?** — All five agent prompts (`planner`, `implementer`, `researcher`, and the new `architecture-reviewer`, `plan-verifier`) carry a "stop and ask first" gate written as if the agent can prompt mid-run, and `planner.md:41` even supplies a fenced question template. But none of their `tools:` allowlists includes `AskUserQuestion`, so in this repo asking can only mean *ending the turn with the question as the report* — which is a different control flow: the caller must notice and re-invoke, and a background agent's question may sit unread. Checked: a `researcher` subagent run this session hit a plan-mode system reminder telling it to use `Write` and `AskUserQuestion`, reported neither was in its toolset, and proceeded — but that agent's allowlist excluded both anyway, so it proves nothing about whether the tool is available to subagents that *do* list it. External docs were read as saying `AskUserQuestion` is stripped from every subagent unconditionally, but that came from a summarised fetch, not a verbatim quote. What would answer it: add `AskUserQuestion` to one agent's `tools:` in a scratch copy, invoke it with a deliberately ambiguous request in a new session, and see whether a prompt renders or the tool is silently absent. Until then, write clarify-gates as "make the question your report", not as "ask the user".
  Where: `.claude/agents/planner.md:4` (`tools:` — no `AskUserQuestion`), `.claude/agents/planner.md:41` (the fenced ask template), `.claude/agents/researcher.md:4` (same omission)

- **2026-09-23 · Does a non-agent `README.md` inside `.claude/agents/` get parsed as a subagent?** — `.claude/agents/README.md` has no frontmatter and no `name:`, so it is not a valid agent file; `.claude/skills/README.md` is the precedent that a plain README is tolerated in a `.claude/` collection dir, but that is the skills loader, not the agents loader. Checked: the file loads no differently in this session, because `.claude/agents/` is read at session **start** — so nothing here proves anything either way. What would answer it: open a new session and check whether the agent list shows three entries (planner, implementer, researcher) and no parse warning naming `README`. If it does complain, the fallback is `_README.md` or moving the map to `docs/`.
  Where: `.claude/agents/README.md:1` (no frontmatter — first line is `# Subagents`), `.claude/skills/README.md:1` (the tolerated precedent, different loader)
